// 例5：两个数的较大值（if 分支）
#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;
    if (a >= b)
        cout << a << endl;
    else
        cout << b << endl;
    return 0;
}
